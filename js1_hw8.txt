1. Для практикума из занятия 7 продумать, где можно применить замыкания.
Если замыкание это вызов функции в контексте другой, думаю можно применить в функции move при проверке ячейки.

2. Не выполняя кода, ответить, что выведет браузер и почему:
if (!("a" in window)) {
    var a = 1;
}
alert(a);
Вернет undefined, тк. переменная a будет не инициализирована. Ветка if пропускается, т.к. в window есть свойство a

var b = function a(x) {
    x && a(--x);
};
alert(a);
Вернет undefined, тк. переменная a не инициализирована.

function a(x) {
    return x * 2;
}
var a;
alert(a);
Переменной а присвоится ссылка на функцию а.

function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);
10, т.к. переопределяется arguments[2]

function a() {
    alert(this);
}
a.call(null);
Объект window, т.к. не указан контекст привязки, то назначается глобальный